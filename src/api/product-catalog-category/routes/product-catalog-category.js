'use strict';

/**
 * product-catalog-category router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::product-catalog-category.product-catalog-category');
